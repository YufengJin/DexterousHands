# DexterousHands — Skill Run History

Generated by `benchmark-env-generator` at **2026-04-24T05:19:17Z**.

This file is a snapshot of what the skill did in this run — probe results, classification evidence, generated artifacts, smoke-test outcomes, final report. It is regenerated on every re-run.

## Metadata

| Field | Value |
|-------|-------|
| Target repo | `DexterousHands` |
| Repo commit | `dc41afd` |
| Upstream skill | `env-generator` (base docker/) |
| Sub-skill | `benchmark-env-generator` |
| Benchmark category | **RL** |
| Baselines mode | `dispatcher` |
| Special flag | `IS_ISAACGYM=1` (Python 3.8, sibling mount, no generic run_demo/run_eval/train_rl_policy) |

## Classifier evidence

**Main criterion** (`env.step()` returns a reward / `compute_reward` exists): PASS — every `bidexhands/tasks/*.py` defines `def compute_reward(self, actions)` and writes to `self.rew_buf[:]`. Example: `bidexhands/tasks/shadow_hand_over.py` → `compute_hand_reward(...)` with `dist_reward_scale` / `rot_reward_scale` / `action_penalty_scale`.

**Auxiliary signals** (8-signal RL-readiness rubric):

| # | Signal | Evidence | Score |
|---|--------|----------|-------|
| 1 | VecEnv / parallel envs | IsaacGym GPU-vectorised `BaseTask` with `--num_envs` | 1 |
| 2 | Horizon / max_episode_length | `self.max_episode_length` in every task class | 1 |
| 3 | Shaped reward (non-sparse) | `dist_reward_scale + rot_reward_scale + action_penalty_scale` shaped blend | 1 |
| 4 | Observation space defined | `self.cfg["env"]["numObservations"]` populated from `num_obs_dict` | 1 |
| 5 | Action space defined | `self.cfg["env"]["numActions"]` = 20 (single-hand) or 40 (two-hand) | 1 |
| 6 | In-tree RL algorithms | `bidexhands/algorithms/{rl,marl,metarl,mtrl,offrl}` with PPO/SAC/TD3/TRPO/DDPG/HAPPO/MAPPO/IPPO/HATRPO + BCQ/IQL/TD3-BC + MTPPO/MTSAC/MTTRPO + MAML | 1 |
| 7 | Training entry point | `bidexhands/train.py` + `bidexhands/train_customize.py` + `bidexhands/train_rlgames.py` | 1 |
| 8 | README keywords | README claims "Benchmark environments for Dexterous Hand in NVIDIA IsaacGym" for RL | 1 |

**Aux score**: 8 / 8 → category `RL` (threshold: RL requires main pass **and** aux ≥ 4). Dispatcher mode chosen because `bidexhands/algorithms/rl/{ppo,sac,td3,trpo,ddpg}/` ships in-tree baselines.

## Probe summary

| Flag | Value | Evidence |
|------|-------|----------|
| `PKG_MGR` | `uv` | `uv` static binary copied from `ghcr.io/astral-sh/uv:latest`; venv at `/opt/venv` |
| `NO_UV_LOCK` | `1` | Only `setup.py` + `requirements.txt`; no `uv.lock` present |
| `PYTHON_VERSION` | `3.8` | IsaacGym upstream ships `gym_38.so`; nothing later works |
| `IS_ISAACGYM` | `1` | `bidexhands/utils/config.py` does `from isaacgym import gymapi`; source not pip-installable |
| `CUDA_EXT_BUILD` | `0` | No `nvcc` compile in deps; `torch==1.8.1` gets overridden by IsaacGym install to torch 2.4.1+cu121 |
| `NEEDS_VULKAN` | `0` | IsaacGym uses PhysX + its own GL stack, not Vulkan |
| `THIRD_PARTY_SIM` | `1` | IsaacGym source mounted from `<parent>/isaacgym/`; not cloned/pinned in pyproject |
| `CONFIG_INIT` | `0` | No `macros_private.py` / interactive config prompt on first import |
| `ASSET_BOOTSTRAP` | `0` | Assets shipped in `assets/` directory; no download script |
| `BASELINES_MODE` | `dispatcher` | `bidexhands/algorithms/rl/` ships full PPO/SAC/TD3/TRPO/DDPG implementations |
| `SMOKE_ENV_ID` | `ShadowHandOver` | Smallest headless-safe single-hand task (20 actions, ~16 envs fit in < 10s) |
| `ISAACGYM_COMMUNITY_IMAGE` | `uvarc/isaacgym:1.0.preview4-cuda11` | Auto-extracted by `docker/setup.sh` when sibling `isaacgym/` missing |

## Generated / edited files

| Path | Origin | Purpose |
|------|--------|---------|
| `docker/Dockerfile` | upstream `env-generator` | cuda 11.8 runtime + uv venv (Python 3.8) + requirements.txt + explicit numpy pin |
| `docker/entrypoint.sh` | upstream `env-generator`, IsaacGym-patched | Clears sentinel → editable-installs isaacgym + bidexhands → writes `/tmp/entrypoint_done` |
| `docker/docker-compose.headless.yaml` | `benchmark-env-generator` (second volume mount added) | Mounts `../:/workspace/DexterousHands` + `../../isaacgym:/workspace/isaacgym` |
| `docker/docker-compose.x11.yaml` | same + X11 + MPLBACKEND=Agg | GUI variant sharing `container_name` |
| `docker/setup.sh` | `benchmark-env-generator` | Auto-extracts IsaacGym via `docker pull` + `docker cp`, builds, waits for sentinel, runs smoke |
| `docker/smoke_test.sh` | `benchmark-env-generator` | Layer 1 (7 env checks) + Layer 2 (3 sim checks including 1-iter training) |
| `docker/README.md` | `benchmark-env-generator` | Day-to-day ops: build, run, smoke, train |
| `install.md` | Step 7 | Minimum reproduction recipe |
| `history.md` | Step 7 | This file |
| `benchmark.md` | Step 7 | Task inventory + how-to walk-through |

Skipped for IsaacGym per skill contract: `scripts/run_demo.py`, `scripts/run_eval.py`, `scripts/train_rl_policy.py`, `tests/test_random_policy_server.py`, `conf/`.

## Tests executed

| Tier | Command | Result |
|------|---------|--------|
| L1 (env + step) | Layer-1 of `docker/smoke_test.sh`: GPU / Python 3.8 / venv / torch+cuda / numpy / bidexhands import | **PASS (7/7)** |
| L2 (reward finite) | Train.py iteration 0/1 reports `Mean reward/step: 0.05` (finite, non-NaN) and `Value function loss: 2.5140` | **PASS** |
| L3_RL (IsaacGym: native train.py) | `cd bidexhands && python train.py --task ShadowHandOver --algo ppo --num_envs 16 --headless --max_iterations 1` — delegated to benchmark's own entry point (no generated `scripts/train_rl_policy.py`) | **PASS** (Learning iteration 0/1 reached; 128 timesteps completed) |

Captured last-5 stdout lines per tier:

**L1 (Layer 1 tail):**
```
[PASS] torch.cuda.is_available() = True
[PASS] numpy: 1.23.0
[PASS] bidexhands editable install: /workspace/DexterousHands/bidexhands/__init__.py
```

**L2 / L3_RL (train.py iteration 0/1 tail):**
```
                        Learning iteration 0/1
               Value function loss: 2.5140
             Mean action noise std: 0.80
                  Mean reward/step: 0.05
       Mean episode length/episode: 8.00
            Mean episode successes: 0.0000
                   Total timesteps: 128
                    Iteration time: 0.28s
```

## Capabilities probed in the container

| Check | Expected | Result |
|-------|----------|--------|
| `nvidia-smi` | GPU visible | **PASS** (NVIDIA GeForce RTX 4090) |
| `python -c "import torch; print(torch.cuda.is_available())"` | `True` | **PASS** (torch 2.4.1+cu121, cuda True) |
| `python -c "import bidexhands; print(bidexhands.__file__)"` | resolves under `/workspace/DexterousHands/` | **PASS** (`/workspace/DexterousHands/bidexhands/__init__.py`) |
| `python -c "import isaacgym"` | import succeeds | **PASS** (editable install at `/workspace/isaacgym/python/`) |
| `python -c "import numpy; print(numpy.__version__)"` | `1.23.0` | **PASS** (`1.23.0` — explicit pin held through requirements install) |

## Final report

Smoke test **10/10 PASS**. The container is runnable end-to-end: `bash docker/setup.sh` builds, starts the container, waits for the entrypoint sentinel, and confirms a 1-iteration PPO training run completes against `ShadowHandOver` with 16 parallel envs on an RTX 4090. The known IsaacGym upstream segfault at `gym.destroy()` is documented and does not affect training correctness (training iteration completes and prints reward / loss / timesteps before shutdown).

### Known limitations

- **IsaacGym shutdown segfault**: benign, known upstream issue. The smoke test asserts on `"Learning iteration 0/1"` presence in stdout, not on exit code. Any production training job should treat SIGSEGV on `gym.destroy()` as end-of-run, not failure.
- **`torch==1.8.1` from `requirements.txt` gets replaced**: IsaacGym's editable install pulls `torch==2.4.1+cu121`. The `requirements.txt` pin is effectively advisory.
- **No generated generic-RL scripts**: `scripts/run_demo.py`, `scripts/run_eval.py`, `scripts/train_rl_policy.py` are **intentionally absent** per skill IsaacGym contract. Use `bidexhands/train.py` directly.
- **`docker/smoke_test.sh` naming**: uses `Layer 1` / `Layer 2` labels from the original generation run rather than the skill's current `L1` / `L2` / `L3_RL` tier names. Content coverage is equivalent.

### Next actions the user can take

```bash
# Work inside the container
docker exec -it dexterousnhands_container bash

# Stop / restart
docker compose -f docker/docker-compose.headless.yaml down
docker compose -f docker/docker-compose.headless.yaml up -d

# Train a policy for real
docker exec -it dexterousnhands_container bash -c \
    "cd /workspace/DexterousHands/bidexhands && \
     python train.py --task ShadowHandOver --algo ppo --num_envs 4096"
```

---

### How this file is regenerated

Re-run `benchmark-env-generator` against the repo. The skill overwrites `install.md`, `history.md`, and `benchmark.md`. To diff before overwriting, rename the old copies with a `.previous.md` suffix first.
